apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: mbti-personality-test
  namespace: mbti-test

resources:
- namespace.yaml
- deployment.yaml
- service.yaml
- ingress.yaml
- configmap.yaml
- hpa.yaml

images:
- name: omaticaya/lovetest-mbti
  newTag: latest

commonLabels:
  app.kubernetes.io/name: mbti-personality-test
  app.kubernetes.io/instance: production
  app.kubernetes.io/version: "1.0.0"
  app.kubernetes.io/component: frontend
  app.kubernetes.io/part-of: mbti-system

namespace: mbti-test