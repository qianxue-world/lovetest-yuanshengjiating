apiVersion: v1
kind: Service
metadata:
  name: mbti-personality-test-service
  namespace: lovetest-mbti
  labels:
    app: mbti-personality-test
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: mbti-personality-test